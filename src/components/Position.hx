package components;

import nme.geom.Point;

class Position
{
    public var x:Int;
    public var y:Int;

    public function new(x:Int = 0, y:Int = 0)
    {
        this.x = x;
        this.y = y;
    }
}
